# AI Research OS

One-page AI research journal website built with Next.js, TypeScript, TailwindCSS and Framer Motion.

## Commands

```bash
npm install
npm run dev
npm run typecheck
npm run build
```

## Content

All editable content lives in `content/*.json`. Section components live in `components/`, and extracted WebP assets live in `public/assets/`.
